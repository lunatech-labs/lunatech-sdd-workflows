---
description: Start or resume the spec-swarm spec-driven development workflow
argument-hint: [feature description, or blank to resume]
---

Invoke the **spec-swarm** skill now.

User input: $ARGUMENTS

If the input describes a feature, begin Phase 1 (SPECIFY) for it. If blank,
run Phase 0 (Orient): look for existing specs under `specs/` with unchecked
tasks and offer to resume the most recent one.
